import type { UserObjectType } from '@dhaaga/core';

type Props = {
	item: UserObjectType;
};

/**
 * An alternative layout, since at proto
 * does not offer profile stats
 * @param props
 * @constructor
 */
function UserListItemViewBsky(props: Props) {}

export default UserListItemViewBsky;
