import { memo } from 'react';
import { View } from 'react-native';

/**
 * NOTE: Dhaaga will not support achievement
 * notification type
 */
const AchiEarnedNotificationFragment = memo(() => {
	return <View />;
});

export default AchiEarnedNotificationFragment;
