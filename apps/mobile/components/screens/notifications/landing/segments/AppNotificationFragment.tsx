import { memo } from 'react';
import { View } from 'react-native';

/**
 * Disabled for Dhaaga
 *
 * Reintroduction heavily depends on
 * user feedback
 */
const AppNotificationFragment = memo(() => {
	return <View />;
});

export default AppNotificationFragment;
