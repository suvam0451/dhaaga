export type ThreadsUser = {
  pk: string
  id: string
  username: string
  is_verified: boolean
  profile_pic_url: string
  FollowerCount: number
  FavouritedLocal: boolean
  FullName: string
}